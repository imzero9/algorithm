// O(1) - Constant Time
// It doesn't matter how big our inputs are, we're always going to do the constant amount of time

function compressFirstBox(boxes) {
    console.log(boxes[0]);
    // console.log(boxes[1])
}
