// Write a function that reverses a string. The input string is given as an array of characters s and the output is an array.

// You must do this by modifying the input array in-place with O(1) extra memory.

const reverseString = (s) => {
    return s.reverse();
};
